# Oryfin App

Oryfin is a financial advisor application designed to help users understand, manage, and optimize their finances.

## 🚀 Project Structure

- web → frontend (React + Vite)
- api → backend (Node.js + Express)

## 📦 Installation

Install dependencies for both projects:

```bash
cd web && npm install
cd ../api && npm install